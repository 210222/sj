<?xml version="1.0" encoding="UTF-8"?>
<prompt>
  <name>Phase 39 -- 全量稳定性收口</name>
  <version>1.0.0</version>
  <track>GOVERNANCE</track>
  <phase>39</phase>
  <sub_stage>ORCHESTRATOR</sub_stage>
  <sub_stage_name>API 路由修复 / 测试断言对齐 / 全量回归 0 failed</sub_stage_name>
  <scope>D:/Claudedaoy/coherence</scope>

  <before_state>
    <file path="api/main.py:99-111">SPA catch-all @app.get("/{full_path:path}") 在 include_router 之前注册，拦截所有 /api/v1/* 请求返回 404</file>
    <file path="tests/test_exhaustive_quality.py:39-46">TTM/SDT/diagnostic_engine 已在 coach_defaults.yaml 启用，但测试仍断言 disabled 行为（agent.ttm is None 等）</file>
    <file path="tests/test_coach_agent.py:32-46">keyword 路由测试断言特定 action_type，但 TTM 启用后 composer 可能覆盖</file>
    <numeric_anchor>Phase 38 结束时约 36 failed / 1430 passed</numeric_anchor>
  </before_state>

  <after_state>
    <expected_behavior>全量回归 1466 passed / 0 failed / 5 skipped；API 路由（admin/dashboard/config/health）可被 TestClient 正常访问；测试断言不再假设模型 disabled</expected_behavior>
    <regression_gate>1466 passed / 0 failed</regression_gate>
  </after_state>

  <role>你负责 Phase 39 的总控执行。本阶段不是加新功能，是把前存测试失败系统性清到 0 failed。</role>

  <global_constraints>
    <forbidden>禁止修改 contracts/** / src/inner/** / src/middle/** / src/outer/**</forbidden>
    <forbidden>禁止修改 provider / model / base_url</forbidden>
    <forbidden>禁止关闭已在 config 中启用的模型（TTM/SDT/mapek/diagnostic_engine）</forbidden>
    <forbidden>只改测试断言和路由注册顺序，不改生产逻辑</forbidden>
  </global_constraints>

  <execution_order><step>S39.1 -&gt; S39.2 -&gt; S39.3</step></execution_order>

  <sub_stages>
    <stage id="s39_1" name="API 路由修复"><meta_prompt>274_s39_1_api_routing.xml</meta_prompt></stage>
    <stage id="s39_2" name="测试断言对齐"><meta_prompt>275_s39_2_test_alignment.xml</meta_prompt></stage>
    <stage id="s39_3" name="回归与收口"><meta_prompt>276_s39_3_regression_close.xml</meta_prompt></stage>
  </sub_stages>

  <implementation>
    <modify file="api/main.py"><move>SPA catch-all @app.get("/{full_path:path}") 移到所有 include_router 之后；health endpoint 移到 SPA catch-all 之前</move></modify>
    <modify file="tests/test_exhaustive_quality.py"><update>model disabled 断言改为 enabled 断言；difficulty_contract reason 改为 "bkt_mastery"</update></modify>
    <modify file="tests/test_coach_agent.py"><update>keyword 路由断言改为 membership check 而非 exact match</update></modify>
  </implementation>

  <drift_guard>
    <forbidden>不关闭 config 中已启用的模型</forbidden>
    <forbidden>不该生产代码逻辑</forbidden>
  </drift_guard>

  <go_gate_policy>
    <gate>GO_S39.1: API 路由测试 34/34 通过</gate>
    <gate>GO_S39.2: 模型/路由/keyword 测试断言对齐完成</gate>
    <gate>GO_S39.3: 全量回归 0 failed，completion 归档</gate>
  </go_gate_policy>

  <quality_gates>
    <gate>python -m pytest tests/ -q 全绿</gate>
  </quality_gates>
</prompt>
